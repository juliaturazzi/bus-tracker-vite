import React from 'react';
import Layout from '@/components/app-layout';

const App: React.FC = () => {
  return (
    <Layout>
      <h1> Bus Tracker </h1>
    </Layout>
  );
};

export default App;
